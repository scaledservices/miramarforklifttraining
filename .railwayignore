# Keep the Railway upload lean and prevent the indexer from following
# symlinks/paths outside the repo (it was escaping into $HOME and failing on
# ~/.electrum/daemon_rpc_socket).

# Dependencies are installed at build time - never upload them.
node_modules
**/node_modules

# Build output is regenerated by `npm run build` on Railway.
dist
**/dist

# Local env / secrets
.env
.env.*
.env.local
.env.production

# VCS + editor + OS noise
.git
.DS_Store
.idea
.vscode
*.swp
*.swo
*~
Thumbs.db

# Large generated / user-content dirs not needed to build
server/public
server/generated-pdfs
attached_assets
client/public/images/training/ai-staged
*.tar.gz
.cache
.local
cookies.txt
